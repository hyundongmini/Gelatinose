# Gelatinose

Static project page, served with GitHub Pages.

- `index.html` — the page
- `support.js`, `image-slot.js` — runtime
- `uploads/` — images and videos

## Deploy
Settings → Pages → Branch: `main` / `/ (root)` → Save.
